/* Config local — copiá este archivo a docs/config.js y completá la key.
 * docs/config.js está en .gitignore: la key nunca se versiona. En
 * producción el workflow lo genera desde el secret CARTO_API_KEY.
 * Sin config.js (o con la key vacía) el mapa anda igual, pero los tiles de
 * CARTO salen con la marca de agua "API KEY REQUIRED". */
window.FARMAGUARDIA_CONFIG = {
  cartoKey: '',
};
